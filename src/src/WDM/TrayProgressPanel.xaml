<Window x:Class="WDM.TrayProgressPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WDM Progress"
        Width="120"
        Height="22"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent"
        AllowsTransparency="True"
        ShowActivated="False"
        Focusable="False"
        MouseLeftButtonDown="Pill_MouseLeftButtonDown"
        MouseMove="Pill_MouseMove"
        MouseLeftButtonUp="Pill_MouseLeftButtonUp"
        SizeChanged="Window_SizeChanged">
    <Border Background="#121216" BorderBrush="#3f3f46" BorderThickness="1" CornerRadius="11" SnapsToDevicePixels="True">
        <Grid x:Name="ClipGrid" ClipToBounds="True">
            <!-- Progress fill -->
            <Border x:Name="Fill" Background="#3b82f6" Opacity="0.38" HorizontalAlignment="Left"
                    Width="{Binding FillWidth}" />
            <!-- Download progress & speed status text -->
            <TextBlock Text="{Binding StatusText}" FontSize="10.5" FontWeight="Medium"
                       Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"
                       TextTrimming="None" Margin="4,0" />
        </Grid>
    </Border>
</Window>
