<Window x:Class="WDM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WDM.ViewModels"
        Title="Windows Download Manager"
        Icon="Assets/WDM.ico"
        Width="918"
        Height="560"
        MinWidth="760"
        MinHeight="420"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="{DynamicResource Brush.Bg}">
    <Window.Resources>
        <vm:TaskStatusToColorConverter x:Key="StatusColor" />
        <vm:TaskStatusToBrushConverter x:Key="StatusBrush" />
        <vm:TextToVisibilityConverter x:Key="TextToVis" />
        <vm:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis" />
        <vm:RetryVisibilityConverter x:Key="RetryVis" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="ProgressBar.IDM" TargetType="ProgressBar">
            <Setter Property="Height" Value="6" />
            <Setter Property="Background" Value="{DynamicResource Brush.Track}" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Accent}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="Root" ClipToBounds="True">
                            <Border Background="{TemplateBinding Background}" CornerRadius="3" />
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="3"
                                    HorizontalAlignment="Left" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat sidebar link (hover reveal), used by bottom-of-sidebar actions -->
        <Style x:Key="SidebarLinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.SurfaceHover}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.SurfaceAlt}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Control" Command="{Binding OpenAddDialogCommand}" />
        <KeyBinding Key="OemComma" Modifiers="Control" Command="{Binding OptionsCommand}" />
        <KeyBinding Key="Delete" Command="{Binding RemoveCommand}" />
        <KeyBinding Key="Space" Command="{Binding TogglePauseCommand}" />
        <KeyBinding Key="F5" Command="{Binding RetryCommand}" />
    </Window.InputBindings>

    <Grid AllowDrop="True" DragOver="Root_DragOver" Drop="Root_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Classic Menu Bar -->
            <RowDefinition Height="Auto" /> <!-- IDM Stacked Action Toolbar / Ribbon -->
            <RowDefinition Height="1" />    <!-- Divider Line below Ribbon -->
            <RowDefinition Height="*" />    <!-- Main Body: Tree Sidebar + Vertical Divider + Download Grid -->
            <RowDefinition Height="Auto" /> <!-- Bottom Status Bar with Top Divider -->
        </Grid.RowDefinitions>

        <!-- 1. Top Menu Bar -->
        <Border Grid.Row="0" Background="Transparent" Margin="2,0,2,0" Padding="0">
            <Menu Background="Transparent">
                <MenuItem Header="Tasks" Style="{StaticResource MenuItem.Top}">
                    <MenuItem Header="Add New Download..." Command="{Binding OpenAddDialogCommand}" InputGestureText="Ctrl+N" />
                    <Separator />
                    <MenuItem Header="Pause All" Command="{Binding PauseAllCommand}" />
                    <MenuItem Header="Resume All" Command="{Binding StartQueueCommand}" />
                    <Separator />
                    <MenuItem Header="Exit" Click="MenuExit_Click" InputGestureText="Alt+F4" />
                </MenuItem>

                <MenuItem Header="File" Style="{StaticResource MenuItem.Top}">
                    <MenuItem Header="Show Progress Window..." Command="{Binding ShowProgressDialogCommand}" />
                    <MenuItem Header="Properties..." Command="{Binding PropertiesCommand}" />
                    <Separator />
                    <MenuItem Header="Open File" Command="{Binding OpenFileCommand}" />
                    <MenuItem Header="Open Folder" Command="{Binding RevealCommand}" />
                    <Separator />
                    <MenuItem Header="Delete Selected" Command="{Binding RemoveCommand}" InputGestureText="Del" />
                    <MenuItem Header="Delete All Completed" Command="{Binding ClearCompletedCommand}" />
                </MenuItem>

                <MenuItem Header="Downloads" Style="{StaticResource MenuItem.Top}">
                    <MenuItem Header="Start Queue" Command="{Binding StartQueueCommand}" />
                    <MenuItem Header="Stop Queue" Command="{Binding StopQueueCommand}" />
                    <Separator />
                    <MenuItem Header="Browser Extension Setup..." Click="MenuExtension_Click" />
                    <MenuItem Header="Options..." Command="{Binding OptionsCommand}" InputGestureText="Ctrl+," />
                </MenuItem>

                <MenuItem Header="View" Style="{StaticResource MenuItem.Top}">
                    <MenuItem Header="Clear Search" Command="{Binding ClearSearchCommand}" />
                </MenuItem>

                <MenuItem Header="Help" Style="{StaticResource MenuItem.Top}">
                    <MenuItem Header="About WDM..." Command="{Binding AboutCommand}" />
                </MenuItem>
            </Menu>
        </Border>

        <!-- 2. Action Toolbar Ribbon -->
        <Grid Grid.Row="1" Margin="6,2,6,4">
            <DockPanel LastChildFill="True">
                <!-- Search Box (Right-aligned, flat border, simplified) -->
                <Border DockPanel.Dock="Right" VerticalAlignment="Center" Margin="12,0,4,0" Height="30" Width="175"
                        Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Width="Auto" Height="28" Background="Transparent" BorderThickness="0" Padding="8,0" FontSize="12"
                                 VerticalContentAlignment="Center" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Search downloads list">
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Grid>
                                        <TextBlock Text="Search downloads…" FontSize="12" Margin="8,0"
                                                   Foreground="{DynamicResource Brush.TextMuted}" VerticalAlignment="Center"
                                                   Visibility="{Binding Text.IsEmpty, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVis}}" />
                                        <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center" />
                                    </Grid>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                        <Button Grid.Column="1" Style="{StaticResource Button.Icon}" Command="{Binding ClearSearchCommand}"
                                Visibility="{Binding HasSearchText, Converter={StaticResource BoolToVis}}" Width="16" Height="16" ToolTip="Clear search" Margin="2,0">
                            <TextBlock Text="&#xF0349;" FontFamily="{StaticResource Font.Icon}" FontSize="9" Foreground="{DynamicResource Brush.TextDim}" />
                        </Button>
                        <TextBlock Grid.Column="2" Text="&#xF0236;" FontFamily="{StaticResource Font.Icon}" FontSize="11"
                                   Foreground="{DynamicResource Brush.TextDim}" Margin="4,0,6,0" VerticalAlignment="Center" />
                    </Grid>
                </Border>

                <!-- Search context indicator (search active inside a category filter) -->
                <Grid DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,4,0" Height="22">
                    <Border Background="{DynamicResource Brush.AccentSoft}" CornerRadius="10" Padding="8,2" Visibility="{Binding SearchContextText, Converter={StaticResource TextToVis}}">
                        <TextBlock Text="{Binding SearchContextText}" FontSize="10" FontWeight="Medium"
                                   Foreground="{DynamicResource Brush.Accent}" VerticalAlignment="Center" />
                    </Border>
                </Grid>

                <!-- Stacked Action Toolbar Buttons -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Group 1. Primary Action: Add Download -->
                    <Button Style="{StaticResource Button.IDMPrimary}" MinWidth="120" Command="{Binding OpenAddDialogCommand}" ToolTip="Add Download (Ctrl+N)">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="&#xF1100;" FontFamily="{StaticResource Font.Icon}" FontSize="16" Foreground="#FFFFFF" Margin="0,0,6,0" VerticalAlignment="Center" />
                            <TextBlock Text="Add Download" FontSize="11.5" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Border Width="1" Height="28" Background="{DynamicResource Brush.Border}" Opacity="0.35" Margin="4,0,6,0" VerticalAlignment="Center" />

                    <!-- Group 2. Download Execution Controls -->
                    <Button Style="{StaticResource Button.IDMStacked}" Command="{Binding ResumeCommand}" ToolTip="Resume (Space)">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0,2,0,0">
                            <TextBlock Text="&#xF040A;" Style="{StaticResource Toolbar.Icon}" Foreground="{DynamicResource Brush.StatusActive}" HorizontalAlignment="Center" Margin="0,0,0,0" />
                            <TextBlock Text="Resume" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource Button.IDMStacked}" Command="{Binding PauseCommand}" ToolTip="Pause (Space)">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0,2,0,0">
                            <TextBlock Text="&#xF03E4;" Style="{StaticResource Toolbar.Icon}" Foreground="{DynamicResource Brush.StatusPaused}" HorizontalAlignment="Center" Margin="0,0,0,0" />
                            <TextBlock Text="Pause" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource Button.IDMStacked}" Command="{Binding PauseAllCommand}" ToolTip="Stop All">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0,2,0,0">
                            <TextBlock Text="&#xF04DB;" Style="{StaticResource Toolbar.Icon}" Foreground="{DynamicResource Brush.StatusPaused}" HorizontalAlignment="Center" Margin="0,0,0,0" />
                            <TextBlock Text="Stop All" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Border Width="1" Height="28" Background="{DynamicResource Brush.Border}" Opacity="0.35" Margin="5,0,5,0" VerticalAlignment="Center" />

                    <!-- Group 3. Deletion Tools -->
                    <Button Style="{StaticResource Button.IDMStacked}" Command="{Binding RemoveCommand}" ToolTip="Delete (Del)">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0,2,0,0">
                            <TextBlock Text="&#xF0A7A;" Style="{StaticResource Toolbar.Icon}" Foreground="{DynamicResource Brush.StatusFailed}" HorizontalAlignment="Center" Margin="0,0,0,0" />
                            <TextBlock Text="Delete" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource Button.IDMStacked}" Command="{Binding ClearCompletedCommand}" ToolTip="Delete Completed">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0,2,0,0">
                            <TextBlock Text="&#xF0C62;" Style="{StaticResource Toolbar.Icon}" Foreground="{DynamicResource Brush.StatusFailed}" HorizontalAlignment="Center" Margin="0,0,0,0" />
                            <TextBlock Text="Clear" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Border Width="1" Height="28" Background="{DynamicResource Brush.Border}" Opacity="0.35" Margin="5,0,5,0" VerticalAlignment="Center" />

                    <!-- Group 4. Theme & About -->
                    <Button Style="{StaticResource Button.IDMStacked}" Command="{Binding OptionsCommand}" ToolTip="Settings (Ctrl+,)">
                        <TextBlock Text="&#xF08BB;" Style="{StaticResource Toolbar.Icon}" Foreground="{DynamicResource Brush.TextMuted}" />
                    </Button>

                    <Button Style="{StaticResource Button.IDMStacked}" Command="{Binding ToggleThemeCommand}" ToolTip="{Binding ThemeButtonToolTip}">
                        <TextBlock Text="{Binding ThemeButtonIcon}" Style="{StaticResource Toolbar.Icon}" Foreground="{DynamicResource Brush.TextMuted}" />
                    </Button>

                    <Button Style="{StaticResource Button.IDMStacked}" Command="{Binding AboutCommand}" ToolTip="About WDM">
                        <TextBlock Text="&#xF02FD;" Style="{StaticResource Toolbar.Icon}" Foreground="{DynamicResource Brush.TextMuted}" />
                    </Button>

                </StackPanel>
            </DockPanel>
        </Grid>

        <!-- 3. Ribbon Divider Line -->
        <Border Grid.Row="2" Height="1" Background="{DynamicResource Brush.Border}" SnapsToDevicePixels="True" UseLayoutRounding="True" />

        <!-- 4. Main Body (Categories Tree Sidebar + Vertical Divider + Seamless IDM Download DataGrid Table) -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarColumn" Width="155" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Categories Tree Sidebar — tighter & better styled -->
            <Border Grid.Column="0" Background="{DynamicResource Brush.Sidebar}" Padding="4,4">
                <DockPanel LastChildFill="True">
                    
                    <!-- Bottom App Options (Moved from Ribbon) -->
                    <StackPanel DockPanel.Dock="Bottom" Margin="4,10,4,4">
                        <Border Height="1" Background="{DynamicResource Brush.Border}" Margin="0,0,0,8" Opacity="0.3" />


                        
                        <Button Click="MenuExtension_Click" ToolTip="Browser Extension Setup" Style="{StaticResource SidebarLinkButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xF059F;" FontFamily="{StaticResource Font.Icon}" FontSize="14" Foreground="{DynamicResource Brush.TextDim}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="Browser Integration" FontSize="11" Foreground="{DynamicResource Brush.TextMuted}" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                    </StackPanel>

                    <ListBox ItemsSource="{Binding Filters}"
                             SelectedValue="{Binding SelectedFilter, Mode=TwoWay}"
                             SelectedValuePath="Kind"
                             Style="{StaticResource ListBox.Sidebar}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,1">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="ToolTip" Value="{Binding Name}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                <Setter Property="ToolTip" Value="{x:Null}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                <Setter Property="ToolTip" Value="{x:Null}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Section Header Label (micro, uppercase) -->
                                <TextBlock Grid.ColumnSpan="3" Text="{Binding HeaderText}"
                                           FontSize="9.5" FontWeight="Bold" Foreground="{DynamicResource Brush.TextMuted}"
                                           Margin="4,6,0,3" VerticalAlignment="Center"
                                           Visibility="{Binding IsHeader, Converter={StaticResource BoolToVis}}" />

                                <!-- Category Divider -->
                                <Border Grid.ColumnSpan="3" Height="1" Background="{DynamicResource Brush.Border}" Margin="0,3" VerticalAlignment="Center" Opacity="0.3"
                                        Visibility="{Binding IsSeparator, Converter={StaticResource BoolToVis}}" />

                                <!-- Category Icon -->
                                <TextBlock Grid.Column="0" Text="{Binding Icon}" FontFamily="{StaticResource Font.Icon}" FontSize="13"
                                           Foreground="{DynamicResource Brush.TextDim}" Margin="2,0,8,0" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCategory}" Value="True">
                                                    <Setter Property="Foreground" Value="{Binding CategoryBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- Category Name -->
                                <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="11.5" VerticalAlignment="Center" TextTrimming="CharacterEllipsis">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- Category Count Pill Badge -->
                                <Border Grid.Column="2" VerticalAlignment="Center" Margin="4,0,2,0" Padding="5,1" CornerRadius="8" Background="{DynamicResource Brush.SurfaceAlt}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding CountText}" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding CountText}" FontSize="10" FontWeight="Medium" Foreground="{DynamicResource Brush.TextMuted}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                </DockPanel>
            </Border>

            <!-- Vertical Divider Line between Sidebar and DataGrid -->
            <Border Grid.Column="1" Width="1" Background="{DynamicResource Brush.Border}" SnapsToDevicePixels="True" UseLayoutRounding="True" />

            <!-- Seamless IDM Download DataGrid Table -->
            <Grid Grid.Column="2" Background="{DynamicResource Brush.Bg}">
                <DataGrid x:Name="TaskGrid"
                      ItemsSource="{Binding TasksView}"
                      SelectedItem="{Binding SelectedTask}"
                      SelectionChanged="TaskGrid_SelectionChanged"
                      IsReadOnly="True"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      RowHeaderWidth="0"
                      RowHeight="36"
                      HeadersVisibility="Column"
                      SelectionMode="Extended"
                      CanUserSortColumns="False"
                      CanUserReorderColumns="False"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{DynamicResource Brush.GridLine}"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      MouseDoubleClick="TaskGrid_MouseDoubleClick">
                <DataGrid.InputBindings>
                    <KeyBinding Key="A" Modifiers="Control" Command="SelectAll" />
                </DataGrid.InputBindings>
                <DataGrid.ContextMenu>
                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="Open" Command="{Binding OpenFileCommand}" FontWeight="Bold">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF03CB;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Open folder" Command="{Binding RevealCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF0770;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator />
                        <MenuItem Header="Resume" Command="{Binding ResumeCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF040A;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Pause" Command="{Binding PauseCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF03E4;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Restart" Command="{Binding RetryCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF0709;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Stop" Command="{Binding StopCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF04DB;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Refresh link..." Command="{Binding RefreshLinkCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF0339;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator />
                        <MenuItem Header="Copy URL" Command="{Binding CopyUrlCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF018F;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator />
                        <MenuItem Header="Priority">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF0603;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                            <MenuItem Header="Low" Command="{Binding SetPriorityCommand}" CommandParameter="Low" />
                            <MenuItem Header="Normal" Command="{Binding SetPriorityCommand}" CommandParameter="Normal" />
                            <MenuItem Header="High" Command="{Binding SetPriorityCommand}" CommandParameter="High" />
                        </MenuItem>
                        <MenuItem Header="Move up" Command="{Binding MoveUpCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF005D;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Move down" Command="{Binding MoveDownCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF0045;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator />
                        <MenuItem Header="Properties..." Command="{Binding PropertiesCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF11E8;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Delete" Command="{Binding RemoveCommand}" InputGestureText="Del">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF0A7A;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.StatusFailed}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Delete with file" Command="{Binding RemoveWithFileCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF0A7A;" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.StatusFailed}" />
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <!-- File Name + Icon -->
                    <DataGridTemplateColumn Header="File Name" Width="*" MinWidth="260">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,6,0">
                                    <TextBlock Text="{Binding TypeIcon}" FontFamily="{StaticResource Font.Icon}" FontSize="12" Foreground="{DynamicResource Brush.TextDim}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding DisplayFileName}" FontWeight="Medium" TextTrimming="CharacterEllipsis" FontSize="12" Foreground="{DynamicResource Brush.Text}" VerticalAlignment="Center" ToolTip="{Binding DisplayFileName}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Size -->
                    <DataGridTemplateColumn Header="Size" Width="85" HeaderStyle="{StaticResource Header.Center}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SizeText}" FontFamily="Consolas, monospace" FontSize="11.5"
                                           Foreground="{DynamicResource Brush.TextDim}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="6,0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Status Column -->
                    <DataGridTemplateColumn Header="Status" Width="115" HeaderStyle="{StaticResource Header.Center}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,0">
                                    <Ellipse Width="7" Height="7" Margin="0,0,7,0" VerticalAlignment="Center"
                                             Fill="{Binding Status, Converter={StaticResource StatusBrush}}" />
                                    <TextBlock Text="{Binding StatusText}" FontSize="11.5" FontWeight="Medium"
                                               Foreground="{Binding Status, Converter={StaticResource StatusBrush}}"
                                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Time left -->
                    <DataGridTemplateColumn Header="Time left" Width="80" HeaderStyle="{StaticResource Header.Right}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Eta}" FontFamily="Consolas, monospace" FontSize="11.5"
                                           Foreground="{DynamicResource Brush.TextDim}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="6,0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Transfer rate -->
                    <DataGridTemplateColumn Header="Transfer rate" Width="105" HeaderStyle="{StaticResource Header.Right}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SpeedText}" FontFamily="Consolas, monospace" FontSize="11.5"
                                           Foreground="{DynamicResource Brush.Accent}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="6,0,12,0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Last Try Date -->
                    <DataGridTemplateColumn Width="110" HeaderStyle="{StaticResource Header.Right}">
                        <DataGridTemplateColumn.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="Last Try Date" />
                            </DataTemplate>
                        </DataGridTemplateColumn.HeaderTemplate>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding AddedAt, StringFormat='{}{0:MMM dd HH:mm}'}"
                                           Style="{StaticResource Cell.RightDim}" Margin="6,0,12,0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>

            <!-- Simplified Empty State Overlay -->
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center"
                  Visibility="{Binding HasNoVisibleTasks, Converter={StaticResource BoolToVis}}">
                <Border Padding="32,24" Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="8">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Width="44" Height="44" CornerRadius="22" Background="{DynamicResource Brush.AccentSoft}" HorizontalAlignment="Center" Margin="0,0,0,12">
                            <TextBlock Text="&#xF01DA;" FontFamily="{StaticResource Font.Icon}" FontSize="20" Foreground="{DynamicResource Brush.Accent}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="{Binding EmptyStateTitle}" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,4" />
                        <TextBlock Text="{Binding EmptyStateSubtitle}" Foreground="{DynamicResource Brush.TextDim}" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,14" />
                        <Button Content="+ Add URL" Style="{StaticResource Button.Accent}" Command="{Binding OpenAddDialogCommand}" HorizontalAlignment="Center" MinWidth="120" Height="32" />
                    </StackPanel>
                </Border>
            </Grid>
            </Grid>
        </Grid>

        <!-- 5. Bottom Status Bar -->
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="1" />
                <RowDefinition Height="24" />
            </Grid.RowDefinitions>
            <!-- Top Divider Line for Status Bar -->
            <Border Grid.Row="0" Height="1" Background="{DynamicResource Brush.Border}" SnapsToDevicePixels="True" UseLayoutRounding="True" />
            <Border Grid.Row="1" Background="{DynamicResource Brush.Bg}" Padding="10,0">
                <DockPanel VerticalAlignment="Center">
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <Canvas x:Name="SparklineCanvas" Width="80" Height="14" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock Text="{Binding StatusRightText}" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" VerticalAlignment="Center" />
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>

