<Window x:Class="WDM.TrayProgressPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WDM Progress"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent"
        AllowsTransparency="True"
        ShowActivated="False"
        Focusable="False"
        MouseLeftButtonDown="Pill_MouseLeftButtonDown"
        MouseMove="Pill_MouseMove"
        MouseLeftButtonUp="Pill_MouseLeftButtonUp">
    <!-- Unobtrusive, quiet Windows desktop status indicator -->
    <Border Background="{DynamicResource Brush.Surface}"
            BorderBrush="{DynamicResource Brush.Border}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="8,3"
            SnapsToDevicePixels="True">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <!-- Small Fluent download/activity icon -->
            <TextBlock Text="&#xF01DA;" FontFamily="{StaticResource Font.Icon}" FontSize="12"
                       Foreground="{DynamicResource Brush.TextDim}" Margin="0,0,5,0" VerticalAlignment="Center" />

            <!-- Plain typography: "66% · 17.6 MB/s" -->
            <TextBlock Text="{Binding StatusText}"
                       FontFamily="{StaticResource Font.App}"
                       FontSize="12.5"
                       FontWeight="Normal"
                       Foreground="{DynamicResource Brush.Text}"
                       VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</Window>
