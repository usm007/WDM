<Window x:Class="WDM.CloudflareChallengeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Cloudflare Protection Solver — WDM"
        Height="580"
        Width="820"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        RenderOptions.ClearTypeHint="Enabled"
        Background="{DynamicResource Brush.Surface}"
        Foreground="{DynamicResource Brush.Text}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Header -->
            <RowDefinition Height="*" />    <!-- WebView2 Content -->
            <RowDefinition Height="Auto" /> <!-- Bottom Action Strip -->
        </Grid.RowDefinitions>

        <!-- Top Instructions Header -->
        <Border Grid.Row="0" Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1" Padding="18,14">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="&#xF0026;" FontFamily="{StaticResource Font.Icon}" FontSize="20" Foreground="{DynamicResource Brush.Accent}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBlock Text="Cloudflare Verification Required" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock x:Name="StatusText"
                           Text="Complete the Cloudflare check below. WDM will automatically capture clearance cookies and resume your download."
                           FontSize="13" Foreground="{DynamicResource Brush.TextMuted}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <!-- WebView2 Challenge Browser -->
        <Grid Grid.Row="1">
            <wv2:WebView2 x:Name="WebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            <Border x:Name="LoadingOverlay" Background="{DynamicResource Brush.Surface}" Visibility="Visible">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="&#xF0349;" FontFamily="{StaticResource Font.Icon}" FontSize="24" Foreground="{DynamicResource Brush.Accent}" HorizontalAlignment="Center" Margin="0,0,0,8" />
                    <TextBlock Text="Initializing browser session…" FontSize="13" Foreground="{DynamicResource Brush.TextDim}" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Bottom Action Strip -->
        <Border Grid.Row="2" Background="{DynamicResource Brush.SurfaceAlt}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,1,0,0" Padding="16,10">
            <DockPanel LastChildFill="False">
                <Button Content="Cancel" Style="{StaticResource Button.Secondary}" Click="Cancel_Click" MinWidth="85" Height="28" DockPanel.Dock="Left" IsCancel="True" />
                <Button x:Name="UseCookiesBtn" Content="Apply Clearance &amp; Resume" Style="{StaticResource Button.Accent}" Click="ApplyClearance_Click" Height="28" Padding="16,0" DockPanel.Dock="Right" IsDefault="True" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
